"""Future offline OSM road-graph preparation and access.

TODO:
- define reproducible extraction, projection, topology, and versioning policy;
- preserve permitted offline map attributes needed for routing and road context;
- validate graph connectivity and road-direction semantics before use;
- separate preprocessing-time geospatial dependencies from runtime graph access.
"""
