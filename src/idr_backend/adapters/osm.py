"""Future offline OSM graph and feature-store adapter.

TODO:
- expose versioned offline graph, road attributes, and historical lookup data;
- prevent runtime dependencies on live tile, routing, or traffic APIs;
- provide map/version provenance to map matching and road-context features;
- keep rendering/tile-display concerns within the separately owned frontend.
"""
